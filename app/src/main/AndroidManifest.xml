<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gaetan.renault.mareu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_reu"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_reu"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".ui.create.CreateMeetingActivity"
            android:label="Nouvelle réunion"
            android:parentActivityName=".ui.meetings.MeetingsActivity">
            <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".ui.meetings.MeetingsActivity" />
        </activity>
        <activity android:name=".ui.meetings.MeetingsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>